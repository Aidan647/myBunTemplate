{
  "name": "mybuntemplate",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "prettier": "^3.8.1"
  },
  "peerDependencies": {
    "typescript": "^6.0.2"
  }
}
